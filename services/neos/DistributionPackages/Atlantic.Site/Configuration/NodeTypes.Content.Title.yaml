'Atlantic.Site:Content.Title':
  superTypes:
    Neos.Neos:Content: true
    Neos.NodeTypes.BaseMixins:TextMixin: true

  ui:
    group: 'atlantic'
    label: 'Title'
    icon: 'icon-file-text'

    inspector:
      groups:
        offsets:
          label: 'Offsets'

  properties:
    topOffset:
      type: integer
      defaultValue: 26
      ui:
        label: 'Top offset (in pixels)'
        reloadIfChanged: true
        inspector:
          group: 'offsets'

    bottomOffset:
      type: integer
      defaultValue: 26
      ui:
        label: 'Bottom offset (in pixels)'
        reloadIfChanged: true
        inspector:
          group: 'offsets'
